apiVersion: karpenter.k8s.aws/v1
kind: EC2NodeClass
metadata:
  name: gpu-odcr
spec:
  capacityReservationSelectorTerms:
    - id: ${ODCR_ID}
  tags:
    Name: ${CLUSTER_NAME}-gpu-node-odcr
    karpenter.sh/discovery: ${CLUSTER_NAME}
    node-type: gpu
    purchase-option: odcr
