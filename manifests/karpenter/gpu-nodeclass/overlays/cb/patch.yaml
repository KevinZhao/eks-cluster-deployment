apiVersion: karpenter.k8s.aws/v1
kind: EC2NodeClass
metadata:
  name: gpu-cb
spec:
  capacityReservationSelectorTerms:
    - id: ${CAPACITY_BLOCK_ID}
  tags:
    Name: ${CLUSTER_NAME}-gpu-node-cb
    karpenter.sh/discovery: ${CLUSTER_NAME}
    node-type: gpu
    purchase-option: capacity-block
