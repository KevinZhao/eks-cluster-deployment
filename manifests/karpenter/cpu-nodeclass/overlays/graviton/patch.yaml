apiVersion: karpenter.k8s.aws/v1
kind: EC2NodeClass
metadata:
  name: graviton
spec:
  amiSelectorTerms:
    - name: "amazon-eks-node-al2023-arm64-standard-${K8S_VERSION}-*"
  tags:
    Name: "${CLUSTER_NAME}-karpenter-graviton"
    Architecture: arm64
    karpenter.sh/discovery: "${CLUSTER_NAME}"
