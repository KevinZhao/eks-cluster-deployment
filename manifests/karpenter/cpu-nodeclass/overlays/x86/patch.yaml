apiVersion: karpenter.k8s.aws/v1
kind: EC2NodeClass
metadata:
  name: x86
spec:
  amiSelectorTerms:
    - name: "amazon-eks-node-al2023-x86_64-standard-${K8S_VERSION}-*"
  tags:
    Name: "${CLUSTER_NAME}-karpenter-x86"
    Architecture: amd64
    karpenter.sh/discovery: "${CLUSTER_NAME}"
